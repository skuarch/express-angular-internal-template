const index                  = '/';
const login                  = '/login';
const ctx                    = '/web';
const ctxAll                 = '/web/*';
const pingError              = '/ping/error';
const pingNoAuth             = '/ping/no/auth';
const appNoAuth              = '/app/no/auth';
const error                  = '/error';
const profile                = '/profile';
const logout                 = '/logout';
const proxy                  = '/proxy';
const health                 = '/health';
const accessDenied           = '/access/denied';

module.exports = {
    index,
    login,
    ctx,
    ctxAll,
    pingError,
    pingNoAuth,
    appNoAuth,
    error,
    profile,
    logout,
    proxy,
    health,
    accessDenied
};